public class App {
    public static void main(String[] args) throws Exception {
       /* /* Gamestate game = DemoLevel.create();

        game.movePlayer(Direction.RIGHT); // collect black shard
        game.movePlayer(Direction.DOWN);  // shouldnt be able to switch area
        game.movePlayer(Direction.RIGHT); //exit block
        System.out.println(game.canExit()); 
        */
       //shouldn't be able to exit 
        //there remains a white shard and the player is in BLACK form; it must be in GREY form to exit
    }
}
